var React = require("React");

var Todo = React.createClass({
    render: function () {
        return(
            <div>
                Todo app
            </div>
        )
    }
});

module.exports = Todo;